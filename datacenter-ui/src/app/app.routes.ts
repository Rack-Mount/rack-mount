import { Routes } from '@angular/router';
import { RackComponent } from './modules/data-center/components/rack/rack.component';

export const routes: Routes = [{ path: 'rack/:id', component: RackComponent }];
